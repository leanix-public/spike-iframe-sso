{
  "name": "vite-starter",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode development",
    "upload": "vite build",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "vite": "^3.2.3"
  },
  "dependencies": {
  }
}